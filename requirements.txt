# --- Document parsing (this step) ---
pymupdf          # PDF text extraction + page rendering for OCR
python-docx      # DOCX text/table extraction
pytesseract      # OCR (requires the tesseract-ocr system binary installed separately)
pillow           # image handling for OCR pipeline

# --- LLM extraction (this step) ---
openai           # Gemini exposes an OpenAI-compatible endpoint -- this client talks to it directly
python-dotenv    # loads GEMINI_API_KEY from a local .env file

# --- App / UI ---
streamlit        # web app framework
pandas           # used to render the products table in the UI

# --- Exports ---
openpyxl         # multi-sheet .xlsx export (terms / line items / risk register)

# --- Testing / fixture generation only ---
reportlab        # also used in production for the downloadable PDF report
